# Data

This folder stores local copies of datasets used by the project.

`benchmark/` contains ARFF files from:
https://github.com/deric/clustering-benchmark

The loader can download missing files automatically, but keeping these copies
makes experiments reproducible without depending on a network request.
